
<ul class="breadcrumb">
  <li>当前位置：</li>
  <li><a href="/">首页</a></li>
    <%@cat.cat_paths.each_with_index do |cat,index|%>
        <%if index != @cat.cat_paths.size - 1%>
          <li>
            <%=link_to cat.cat_name,"#{cat_path(cat)}?gtype=#{params[:gtype]}",:target=>"_blank" if cat%>
          </li>
        <%else%>
           <li class="active">
            <%=cat.cat_name if cat%>
          </li>
        <%end%>

    <%end%>
</ul>